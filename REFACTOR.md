# リファクタリング案

このドキュメントは `tsunagiya`
プロジェクトの品質向上を目的としたリファクタリング案をまとめたものです。

## 1. 依存性注入 (DI) の導入によるテスト容易性の向上

**現状:** `Relay` クラスが内部で `WebSocket`
のインスタンスを直接生成しています。これにより、`Relay`
クラスのユニットテストが実際のWebSocket通信に依存してしまい、テストの実行環境が制限されたり、ネットワーク状況によってテストが不安定になったりする可能性があります。

**提案:** `Relay` のコンストラクタで `WebSocket`
のインスタンス（またはWebSocketを生成するファクトリ）を外部から受け取るように変更します（依存性の注入）。

**メリット:**

- **テスト容易性:** テスト時にモックの `WebSocket`
  を注入できるようになり、ネットワーク接続なしで `Relay`
  クラスのロジックを単独でテストできます。
- **柔軟性:** `WebSocket` 以外の通信手段（例:
  テスト用のインメモリ通信）にも対応しやすくなります。

**実装例:**

```typescript
// src/relay.ts (変更後)
interface WebSocketLike {
  // WebSocketのインターフェースを定義
}

class Relay {
  #ws: WebSocketLike;

  constructor(
    url: string,
    options: RelayOptions,
    wsFactory: (url: string) => WebSocketLike,
  ) {
    this.#ws = wsFactory(url);
    // ...
  }
}
```

## 2. Relayの状態管理の明確化（ステートマシンの導入検討）

**現状:** `Relay` クラスは `status` プロパティ (`"connecting"`, `"connected"`,
`"closing"`, `"closed"`) や `WebSocket` の `readyState`
を組み合わせて接続状態を管理しています。ロジックが複雑化するにつれて、状態遷移の管理が煩雑になり、意図しない振る舞いやバグを生む原因となり得ます。

**提案:**
XStateのようなステートマシンライブラリを導入するか、あるいは自前でシンプルなステートマシンを実装し、`Relay`
の接続状態（`connecting`, `connected`, `reconnecting`,
`closed`など）と、その状態遷移（`CONNECT`, `DISCONNECT`,
`ERROR`など）をより宣言的かつ堅牢に管理します。

**メリット:**

- **可読性:**
  状態と遷移が明確に定義されるため、複雑な接続ロジックが理解しやすくなります。
- **堅牢性:** 不正な状態遷移を防ぎ、より予測可能な動作を保証します。
- **保守性:** 新しい状態や遷移の追加が容易になります。

## 3. RelayPoolの責務分割

**現状:** `RelayPool`
は、リレーの管理、イベントの発行、購読の管理など、多くの責務を担っています。

**提案:** `RelayPool` の責務をより小さなクラスに分割することを検討します。

- `RelaySelector`: どのリレーにイベントを送信するかを選択する戦略クラス（例:
  `RoundRobinSelector`, `LatencyBasedSelector`）。
- `SubscriptionManager`: 複数のリレーにまたがる購読を管理するクラス。

**メリット:**

- **関心の分離:**
  各クラスが単一の責務を持つことで、コードの理解と保守が容易になります。
- **拡張性:**
  新しいリレー選択戦略や購読管理方法を、既存のコードに影響を与えることなく追加できます。

## 4. Observableベースのイベントストリーム処理の導入検討

**現状:** 購読は `AsyncIterator` を返す `subscribe`
メソッドによって実現されています。これはシンプルで分かりやすい一方で、より高度なストリーム操作（流量制御、フィルタリング、合成など）を行うには不向きな場合があります。

**提案:**
RxJSのようなObservableベースのライブラリの導入を検討します。`subscribe`
メソッドが `Observable<NostrEvent>`
を返すように変更することで、より宣言的で強力な非同期処理パイプラインを構築できます。

**メリット:**

- **表現力:** `debounceTime`, `filter`, `map`, `merge`
  などの豊富なオペレータを用いて、複雑なイベント処理を簡潔に記述できます。
- **柔軟性:** 複数のイベントストリームを容易に合成・変換できます。

**懸念点:**

- 新たな外部依存ライブラリ（RxJS）が追加されます。プロジェクトの方針と照らし合わせて慎重に検討する必要があります。

## 5. デバッグ体験の向上

**現状:** `console.log`
などでオブジェクトをデバッグする際、デフォルトの出力では内部構造が分かりにくい場合があります。

**提案:** `Deno.customInspect` を主要なクラス (`Relay`, `RelayPool` など)
に実装し、デバッグ時のコンソール出力をより分かりやすく、情報量の多いものにカスタマイズします。

**メリット:**

- **デバッグ効率:** `console.log` するだけで、オブジェクトの現在の状態（接続URL,
  購読数など）が一目で分かります。
